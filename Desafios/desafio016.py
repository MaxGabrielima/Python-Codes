import math 
from math import floor
f = float(input('Digite um número: '))
r= math.floor(f)
print('A porção inteira de {} é {}'.format(f, r))

#também é possível resolver utilizando o comando trunc também do módulo math, ou a função int que é interna do Python
